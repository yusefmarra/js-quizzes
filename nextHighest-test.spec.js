var nextHighest = require('.nextHighest');

describe('nextHighest', function() {
    it('should return an error if only one digit provided', function() {

    });

    it('should flip the digits if two digits given, provide the first digit is lower than the second.', function() {

    });

    it('should return an error if the first digit is higher than second', function() {

    });
})
